# 模组编辑器 TODO（专用）

本文件仅包含模组编辑器（自走棋策划/模组编辑器）相关的待办任务，按优先级分组，便于迭代推进。

---

## 高优先级（必须）

- Fetter 复合数据持久化
  - 目标：完善 Controller/Service/DAO 的复合保存/更新/删除接口（支持复合主键 (id, numofpeople) 或将 effects 存为复合字段）。
  - 要点：实现 `save_fetter`/`update_fetter_composite`/`insert_fetter_composite`/`delete_fetter` 并保证与本地 JSON 或数据库一致。
  - 验收：UI 中的新增/删除门槛、增删改效果操作保存后，重启程序数据不丢失且左侧列表正确显示变体。

- 效果（effect）编辑器完善
  - 目标：`ui_effect.show_effect_editor` 支持传入初始效果并预填充，返回规范化效果对象/字符串。
  - 要点：输入校验、参数格式提示、自然语言预览。
  - 验收：在羁绊编辑界面点击“修改效果”能预填原有值并保存回写。

- 本地化（lang）和 key 管理
  - 目标：统一 lang key 规则，完善 LangDao 增删改接口，UI 新创建/修改时同步更新 lang 文件。
  - 要点：新增 key 列表追踪、删除时清理 lang 文件。
  - 验收：创建/更新/删除角色、羁绊后 lang 文件字典一致，UI 显示使用本地化文本。

---

## 中优先级（重要但可分次完成）

- 羁绊（Fetter）编辑器体验改进
  - 自动生成描述（已实现基础）扩展：支持只生成并插入到 effects 的某个子字段或展示在预览面板而不直接覆盖 description。
  - 撤销/重做：实现简单的 undo stack（对 add/remove/edit 操作）。
  - 批量操作：导入/导出选中羁绊的 effects，批量添加/删除门槛。

- UI 布局与美化
  - 压缩工具栏高度、用小图标替换文本按钮（节省垂直空间）。
  - 门槛条目的可折叠（折叠/展开全部），便于大量门槛的展示。

- 数据格式与兼容
  - 确定 effects 的持久化格式（description 内嵌结构 vs 单独字段 vs 独立 JSON 文件），并实现迁移脚本。
  - 提供 `dumpJson` 与 `loadJson` 功能用于备份/恢复模组数据。

---

## 低优先级（长期或可选）

- 自然语言生成增强
  - 支持合并同前缀效果、数值合并显示、支持中文断句和更可读的短语。
  - 可选：接入简单模板引擎或规则引擎定义 effect -> 文本 的映射。

- 可视化改进
  - 拖拽排序门槛与效果；支持复制粘贴效果条目。
  - 在效果描述处高亮参数（例如括号内数值、百分比）。

- 集成测试与 CI
  - 为 service/dao 编写单元测试，配置简单 CI（GitHub Actions）以执行 lint 和测试。

---

## 开发与验收建议

- 每个任务以小 PR 提交，PR 描述应包含：变更目的、影响文件清单、测试步骤、回滚方法。
- 先完善后端接口（service/dao），再修改 UI，使保存逻辑稳定。
- 对可能破坏已有数据的变更（如 effects 格式迁移）必须提供迁移脚本并在 README 记录。

---

## 参考与备注

- 相关文件：`ui_fetter.py`、`ui_effect.py`、`mod_service.py`、`mod_dao.py`、`mod_controller.py`、`utils.py`。
- 若需按模块分配任务或生成更细的 issue 模板，我可以把每一项拆成更小的任务并写入新的 TODO 文件或 issues 列表。
