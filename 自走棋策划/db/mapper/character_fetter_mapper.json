{
  "table": "character_fetter",
  "primary_key": ["character_id","fetter_id","people_count"],
  "columns": [
    {"name":"character_id","sql_type":"TEXT","nullable":false,"pk":true,"python_type":"str","fk":{"table":"character","columns":["id"]}},
    {"name":"fetter_id","sql_type":"TEXT","nullable":false,"pk":true,"python_type":"str","fk":{"table":"fetter","columns":["id"]}},
    {"name":"people_count","sql_type":"INTEGER","nullable":false,"pk":true,"python_type":"int","fk":{"table":"fetter","columns":["people_count"]}}
  ],
  "ddl": "CREATE TABLE IF NOT EXISTS character_fetter (\n    character_id TEXT NOT NULL,\n    fetter_id TEXT NOT NULL,\n    people_count INTEGER NOT NULL,\n    PRIMARY KEY (character_id, fetter_id, people_count),\n    FOREIGN KEY (character_id) REFERENCES character(id),\n    FOREIGN KEY (fetter_id, people_count) REFERENCES fetter(id, people_count)\n);",
  "templates": {
    "select_by_character": "SELECT * FROM character_fetter WHERE character_id = ?",
    "delete_association": "DELETE FROM character_fetter WHERE character_id = ? AND fetter_id = ? AND people_count = ?",
    "insert_template": "自动生成（基于 columns）"
  }
}